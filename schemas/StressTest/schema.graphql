type Query {
  booleans: [Boolean!]
  floats: [Float!]
  ids: [ID!]
  ints: [Int!]
  strings: [String!]

  customScalar: Scalar

  object: Object
}

type Object {
  enum: ENUM
}

enum ENUM {
  FIRST
  SECOND
}

scalar Scalar
